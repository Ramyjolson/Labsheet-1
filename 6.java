import Java.util.*;
Public ASCIIValue{
	public static void main(string[]args){
	 Scanner Sc=new Scanner(system.in);
	 System.out.print("Enter a character: ");
	 char ch=Sc.next().charAt(0);
	 int ascii==(int)ch;
	 System.out.prinln("ASCII value of " + ch+" is: " + ascii);
	 Sc.close()
	}
}
